apiVersion: v1
kind: Service
metadata:
  name: {{ include "phoenix-app.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "phoenix-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 4000
      protocol: TCP
      name: http
